# Онлайн Скорая — сервер (Node.js)

Готовый скелет репозитория для GitHub. Сюда можно **просто скопировать ваши файлы** (frontend, API и пр.), 
а затем выполнить команды ниже, чтобы всё залить в GitHub.

---

## 🚀 Как запустить локально

1) Откройте консоль (`Win + R` → `cmd`) и перейдите в папку проекта:
```bash
cd ПУТЬ\ДО\online-skoraya
```

2) Установите зависимости (Express, JWT, bcrypt, SQLite, CORS):
```bash
npm install
```

3) (Опционально) создайте файл `.env` по образцу `.env.example` и задайте секреты:
```
JWT_SECRET=change_me
PORT=3000
```

4) Запустите сервер:
```bash
npm start
# или
node server.js
```

5) Откройте в браузере:
```
http://localhost:3000
```
или по IP машины в вашей сети.

---

## 📦 Как «засунуть в гит» (GitHub)

```bash
git init
git add .
git commit -m "Initial commit: Online Skoraya"
git branch -M main
git remote add origin https://github.com/<ВАШ_ЛОГИН>/online-skoraya.git
git push -u origin main
```

> Если репозиторий уже создан на GitHub — скопируйте его URL и подставьте вместо примера.

---

## 📁 Куда класть ваши файлы

- Если у вас уже есть `server.js` — **замените** им этот файл.
- Фронтенд положите в `frontend/` (создайте папку при необходимости).
- БД SQLite появится автоматически в `data/` (файл создастся при первом запуске).

---

## 🧪 Мини‑эндпоинты

- `GET /health` → проверка: `{"status":"ok"}`  
- `POST /auth/register` → демо‑регистрация (в памяти)  
- `POST /auth/login` → демо‑логин (JWT)  
- `GET /secure/profile` → доступ только с `Authorization: Bearer <token>`

> Это базовый каркас, чтобы у вас сразу всё заводилось. Подмените на свою бизнес‑логику.